<Window x:Class="Start.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Start.UI.Views"
        xmlns:vm="clr-namespace:Start.UI.ViewModels"
        mc:Ignorable="d"
        Title="Universal Media Downloader" Height="600" Width="900"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignCardBackground}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock Text="DOWNLOADER" FontWeight="Bold" FontSize="16" Margin="10,20,10,30" HorizontalAlignment="Center"/>
                
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Add Download" Command="{Binding NavigateToAddDownloadCommand}" HorizontalAlignment="Stretch" Margin="0,5"/>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Queue" Command="{Binding NavigateToQueueCommand}" HorizontalAlignment="Stretch" Margin="0,5"/>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="History" Command="{Binding NavigateToHistoryCommand}" HorizontalAlignment="Stretch" Margin="0,5"/>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Settings" Command="{Binding NavigateToSettingsCommand}" HorizontalAlignment="Stretch" Margin="0,5"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
