<UserControl x:Class="Start.UI.Views.AddDownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Start.UI.Views"
             xmlns:vm="clr-namespace:Start.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Section -->
        <materialDesign:Card Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Add New Download" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" 
                             materialDesign:HintAssist.Hint="Paste URL here (YouTube, Facebook, TikTok...)"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             VerticalAlignment="Center" Margin="0,0,10,0"/>
                    
                    <Button Grid.Column="1" Command="{Binding AnalyzeUrlCommand}" 
                            Content="Analyze" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/> 
                </Grid>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,5" Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                
                <ProgressBar IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,10,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Analysis Result -->
        <materialDesign:Card Grid.Row="1" Padding="20" Visibility="{Binding AnalyzedJob, Converter={StaticResource NullToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Image Source="{Binding AnalyzedJob.ThumbnailUrl}" Stretch="UniformToFill" Height="120"/>
                
                <StackPanel Grid.Column="1" Margin="20,0,0,0">
                    <TextBlock Text="{Binding AnalyzedJob.Title}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding AnalyzedJob.Duration}" Margin="0,5"/>
                    
                    <CheckBox
                        Margin="0,10,0,4"
                        Content="Download audio only (MP3)"
                        IsChecked="{Binding IsAudioOnly}" />

                    <ComboBox
                        Margin="0,4"
                        materialDesign:HintAssist.Hint="Select Video Stream"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}"
                        ItemsSource="{Binding VideoStreams}"
                        SelectedItem="{Binding SelectedVideoStream}"
                        DisplayMemberPath="DisplayName"
                        IsEnabled="{Binding IsAudioOnly, Converter={StaticResource InverseBooleanConverter}}" />

                    <ComboBox
                        Margin="0,4"
                        materialDesign:HintAssist.Hint="Select Audio Stream"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}"
                        ItemsSource="{Binding AudioStreams}"
                        SelectedItem="{Binding SelectedAudioStream}"
                        DisplayMemberPath="DisplayName" />
                    
                    <Button Command="{Binding StartDownloadCommand}" Content="Download Now" HorizontalAlignment="Left" Margin="0,10"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

    </Grid>
</UserControl>
